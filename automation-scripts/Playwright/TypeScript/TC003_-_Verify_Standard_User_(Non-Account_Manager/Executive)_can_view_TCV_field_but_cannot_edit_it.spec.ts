[
  {
    "filename": "tests/tc003-verify-standard-user-tcv-field.spec.ts",
    "code": "import { test, expect, Page, Locator } from '@playwright/test';\n\n// IMPORTANT: Configure your Salesforce base URL in playwright.config.ts or as an environment variable.\n// Example in playwright.config.ts:\n// use: {\n//   baseURL: 'https://your-salesforce-org.lightning.force.com',\n// }\n// Or ensure process.env.SALESFORCE_BASE_URL is set.\nconst SALESFORCE_BASE_URL: string = process.env.SALESFORCE_BASE_URL || 'https://example.com'; // Fallback for local testing\n\ntest.describe('TC003 - Verify Standard User TCV Field Access', () => {\n  const accountName: string = 'Gamma Services - TCV Test';\n  const expectedTcvAmount: string = '$25,000.00';\n  const attemptTcvAmountRaw: string = '30000.00';\n\n  test.beforeEach(async ({ page }: { page: Page }) => {\n    // Precondition: User is logged in to Salesforce as a Standard User.\n    // This test assumes login is already handled by a global setup or fixture.\n    // We navigate to the